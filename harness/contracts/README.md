# Harness Contracts

Interface and contract documents live here.

Use this directory for task IDs, CLI arguments, Python entry points, environment names, observation/action spaces, file formats, data schemas, and integration boundaries.

Update the relevant contract when an interface changes.
