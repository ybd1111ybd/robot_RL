# Project State

## Phase
Harness initialization and Isaac Lab reinforcement learning bring-up.

## Repository Layout
- `robot_simulator-main/`: main simulator, Docker, ROS, MuJoCo, and Isaac runtime configuration area.
- `Jz_descripetion-main/`: JZ robot model assets, including URDF, MJCF, meshes, and related description files.
- `jz_isaac_lab/`: Isaac Lab reinforcement learning project mounted into Docker as `/workspace/jz_isaac_lab`.
- `harness/`: AI collaboration harness for task protocols, contracts, runtime context, and decisions.
- `learning_records/`: question-and-answer learning notes that consolidate reusable Isaac Lab and reinforcement-learning debugging lessons.

## Current Technical State
- Isaac Sim / Isaac Lab Docker workflow is being prepared for JZ robot reinforcement learning.
- `jz_isaac_lab` contains JZ Isaac Lab task code, RL-Games scripts, documentation, and task registrations.
- The initial validated training target is `Isaac-Reach-JZ-Bi-v0`.
- A short smoke training run for the Reach task has completed in Docker with RL-Games.
- Smoke checkpoint path:
  `/home/cqy/workspace/middle_platform/robot_simulator/jz_isaac_lab/logs/rl_games/jz_bi_reach/docker_smoke_32x5_no_compile/nn/last_jz_bi_reach_ep_5_rew_-inf.pth`
- Harness documentation now records Docker/Isaac Lab runtime context, RL task contracts, and the Reach training procedure.
- Reach task anatomy has been documented, including task registration, action space, observation space, command sampling, rewards, termination, curriculum, and PPO settings.
- Server migration guidance has been documented for moving the project, Docker image, paths, logs, and validation workflow to another machine.
- Baseline `reach_128env_2000it_v1` completed. Reward improved to about `2.76`, but headless evaluation still shows large TCP errors and zero near-goal ratio, so the policy is not yet a successful reaching policy.
- User video observation showed apparent joint-limit hits and erratic motion. Experimental Easy Reach task `Isaac-Reach-JZ-Bi-Easy-v0` was added to reduce action scale, add command curriculum, and enable progress/goal/action-bound rewards.
- Easy run `reach_easy_128env_2000it_v1` completed. Reward rose to about `36.35`, but headless evaluation worsened actual reaching error and action aggressiveness, so this exact Easy v1 configuration should not be continued.
- Server-specific Docker paths have been migrated to `/mnt/data2/ybd/robot_simulator` in `robot_simulator-main/docker/isaac/.env`.
- OpenArm-style control alignment has been documented in `harness/context/jz_openarm_alignment.md`.
- Experimental Control Reach task `Isaac-Reach-JZ-Bi-Control-v0` has been added to test relative joint-position action semantics before further reward shaping.
- Control probe `reach_control_64env_300it_probe` completed on GPU 3. Evaluation showed poor reach accuracy, especially left-hand error (`left_error_mean` about `0.98`, `right_error_mean` about `0.74`, `near_goal_ratio` zero), but much smoother motion than baseline (`joint_accel_max_abs_mean` about `3.51` vs baseline about `147.53`). Do not blindly continue long training before inspecting left/right TCP definitions, command sampling, target distribution, and action saturation.
- Short-term strategy changed from reward/PPO tuning to environment bring-up first. `harness/tasks/reach_environment_bringup_plan.md` records the plan: validate zero action, joint sweep, TCP/target/reward sanity, then build a conservative 3D position-only Reach task before attempting longer training.
- Zero-action validation clarified that the Control action offset is essentially correct when reset randomization is set to zero correctly. With gravity enabled, default-pose zero action still drifts (`body` about `0.10` rad, arms about `0.21` rad by step 120). With robot gravity disabled, zero-action drift is near zero. This indicates the short-term Reach setup should use no gravity or equivalent gravity compensation before training a 3D position-only policy.
- Short-term PosOnly Reach tasks `Isaac-Reach-JZ-Bi-PosOnly-v0` and `Isaac-Reach-JZ-Bi-PosOnly-Play-v0` have been added for environment-first training. They disable robot gravity, use Control-style relative joint-position actions, disable reset randomization, and expose a small nearest-target workspace curriculum for 3D position-only Reach.
- First PosOnly short run `reach_posonly_64env_300it_v1` reached targets in video but used excessive looping/joint motion. Added Smooth PosOnly tasks to reduce action scale and strengthen action/velocity penalties before running longer training.
- Legacy handoff checkpoint `/workspace/jz_isaac_lab/legacy_checkpoints/legacy_handoff/jz_bi_reach.pth` is now the best available Reach baseline. It is compatible with the original 68-D Reach observation layout. A 300-step evaluation on `Isaac-Reach-JZ-Bi-v0` reached about `0.009m` left and `0.010m` right last-50 TCP error with `near_goal_ratio_last50` about `0.94`, but still has nonzero motion/settling issues.
- Grasp and Drawer handoff environments exist in the active Docker-mounted project under `jz_isaac_lab/source/jzlab/jzlab/tasks/manager_based/jz_manipulation/bimanual/{grasp,drawer}`. Both were smoke-validated on GPU 4 in headless Docker: `Isaac-Grasp-JZ-Bi-v0` initializes with action shape `18` and observation shape `69`; `Isaac-Open-Drawer-JZ-Bi-v0` initializes with action shape `16` and observation shape `72`.
- Grasp/Drawer are not yet training-ready in the same sense as the short-term Reach setup. Zero-action validation shows large arm drift because both tasks still use `JointPositionToLimitsActionCfg` for arm actions, so zero action maps to joint-limit midpoint targets rather than holding the reset pose. Short debug videos were recorded at `jz_isaac_lab/logs/debug_reach_env/grasp_zero_action_env_check_v1/videos/rl-video-step-0.mp4` and `jz_isaac_lab/logs/debug_reach_env/drawer_zero_action_env_check_v1/videos/rl-video-step-0.mp4`.
- As-built Grasp and Drawer RL-Games smoke training was run on GPU 4 with 32 envs for 20 iterations. Grasp completed and saved `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_20it_smoke/nn/last_jz_bi_grasp_ep_20_rew__6.2294226_.pth`. Drawer completed and saved `jz_isaac_lab/logs/rl_games/jz_bi_open_drawer/drawer_asbuilt_32env_20it_smoke/nn/last_jz_bi_open_drawer_ep_20_rew_-inf.pth`, but RL-Games warned that max epochs were reached before any environment terminated, so this Drawer smoke is only a pipeline check, not a meaningful learning result.
- Grasp as-built smoke checkpoint playback video was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_20it_smoke/videos/play/rl-video-step-0.mp4` with video length about `9.97s`.
- As-built Grasp long comparison run `grasp_asbuilt_32env_2000it_v1` completed on GPU 4 with 32 envs. Final checkpoint is `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_2000it_v1/nn/last_jz_bi_grasp_ep_2000_rew_16.711939.pth`; best checkpoint is `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_2000it_v1/nn/jz_bi_grasp.pth`, with the highest observed saved reward about `17.25`. A playback video for the best checkpoint was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_2000it_v1/videos/play/rl-video-step-0.mp4` with video length about `9.97s`.
- Fixed-start Grasp tasks `Isaac-Grasp-JZ-Bi-Fixed-v0` and `Isaac-Grasp-JZ-Bi-Fixed-Play-v0` have been added. They disable robot gravity, fix robot/object reset randomization, use default-pose-offset arm actions with scale `0.20`, and add stronger action/speed plus joint-posture/joint-limit penalties. Zero-action validation showed near-zero arm drift over 120 steps, with body joint drift about `0.025 rad`.
- Fixed-start Grasp run `grasp_fixed_32env_300it_v1` completed on GPU 4 with 32 envs. Best checkpoint is `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_fixed_32env_300it_v1/nn/jz_bi_grasp.pth`; final checkpoint is `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_fixed_32env_300it_v1/nn/last_jz_bi_grasp_ep_300_rew_13.934165.pth`. A playback video for the best checkpoint was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_fixed_32env_300it_v1/videos/play/rl-video-step-0.mp4`.
- Fixed-start Grasp playback was still qualitatively wrong: the robot used odd postures and did not reach the visible target. A Grasp geometry diagnostic showed the fixed object center starts about `1.11m` from the left TCP and `1.03m` from the right TCP, while the reward asks both fingertips to approach the same object center. This makes the task a poor first curriculum for producing natural-looking motion.
- Approach-only Grasp tasks `Isaac-Grasp-JZ-Bi-Approach-v0` and `Isaac-Grasp-JZ-Bi-Approach-Play-v0` have been added. They inherit the fixed-start setup, place a gravity-disabled object at a more reachable training pose, replace the left/right TCP-to-object observations with TCP-to-side-target vectors, train side-target approach rewards, and disable grasp/lift/contact rewards for the first curriculum stage. The new Python files pass syntax checks with `PYTHONPYCACHEPREFIX=/tmp/jz_pycache python -m py_compile ...`; Docker validation still needs to be run from a shell with Docker socket access.
- Grasp training strategy has been reframed as IK-gated RL rather than direct full-task PPO. IK is only a pre-training sanity gate for target geometry, action semantics, and reachable non-pathological postures; PPO should still learn from environment interaction, not follow an IK trajectory as the policy. `harness/tasks/grasp_ik_rl_plan.md` records the staged plan: IK sanity, Approach-only PPO, weak 6D orientation reward, gripper close/contact, then small randomization.
- Added `jz_isaac_lab/scripts/tools/debug_grasp_ik.py` for Stage 1. It starts a Grasp/Approach environment, reads object/TCP state, solves left/right standalone IK targets, prints IK errors and joint targets, and can record an interpolation video from the current pose to the IK joint targets. It supports multi-start IK selection with `--ik_random_starts` and `--min_joint_margin`, so target selection can reject solutions that reach position only by sitting on a joint limit. The script passed local syntax validation with `PYTHONPYCACHEPREFIX=/tmp/jz_pycache python -m py_compile jz_isaac_lab/scripts/tools/debug_grasp_ik.py`.
- Current Grasp IK scan status: smaller side targets such as `side_target_y=0.18` with `target_offset_z=0.15` are numerically reachable but still returned `right joint_margin_ratio=0.0000`. A later limit-aware scan at `side_target_y=0.30`, `target_offset_z=0.20` reached left/right errors about `0.028m` and `0.042m` with reasonable margins, but video validation failed because the right arm took a long rotating path. `debug_grasp_ik.py` now adds path-aware IK gating with `--max_joint_delta` and `--max_joint_delta_norm`; do not promote targets unless error, margin, and path gates all pass and the video looks natural.
- PPO-first Grasp task `Isaac-Grasp-JZ-Bi-Approach-3D-v0` and play variant have been added. This task removes IK from the training path, uses fixed object/robot starts, hand-written 3D side targets near the reset TCP path, disables progress/contact/gripper/lift/orientation rewards, and keeps only 3D TCP tracking plus smoothness/posture/limit penalties. It is the next 2000-iteration Grasp run target.
- The `grasp_approach3d_256env_2000it_v1` video was qualitatively poor and did not reach the target reliably. Do not continue that exact target/reward setup by only adding iterations.
- Easier PPO sanity task `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-v0` and play variant have been added. It now targets the middle bottle object root with zero side/height offset, adds a direct distance penalty, raises arm action scale to `0.25`, strengthens broad tracking, and relaxes smoothness/posture/limit penalties to test whether PPO can first learn visible approach motion toward the bottle.
- Added comparison task `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-Smooth-v0` and play variant. It keeps the same middle-bottle target, distance rewards, action scale, and PPO setup as Easy, but strengthens only the first-layer smoothness/posture penalties: `action_rate=-1.0e-3`, arm `joint_vel=-8.0e-4`, arm `joint_posture=-1.5e-3`, and leaves `action_max_abs=-5.0e-4`, `joint_limit_margin=-5.0e-2`.
- Added comparison task `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-Weighted-v0` and play variant after Easy/Smooth evaluation still showed failure to reach and large action changes. It inherits Easy-Smooth and changes only arm joint-velocity rewards to `mdp.weighted_joint_vel_l2` with weights `[1.5, 1.5, 1.2, 1.2, 0.8, 0.6, 0.5]`.
- Added easier curriculum task `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-v0` and play variant. It uses two fixed visual target cuboids on the table: left object at `(0.62, 0.38, OBJECT_INITIAL_CENTER_Z)` and right object at `(0.62, -0.38, OBJECT_INITIAL_CENTER_Z)`, so the arms no longer compete for the same middle bottle center. The current target offset is `(0, 0, 0)`, meaning each TCP tracks the corresponding object center. It inherits the Easy-Weighted smoothness setup and keeps gripper/contact/lift/orientation rewards disabled.
- RL-Games `play.py` now accepts `--camera_eye` and `--camera_lookat` for headless playback videos. Use a higher camera such as `--camera_eye 3.2,3.4,3.0 --camera_lookat 0.65,0.0,1.35` when the default video view cuts off the upper body.
- RL-Games `play.py` also accepts `--colorize_arms`, `--left_arm_color`, and `--right_arm_color` for playback-only material overrides. Use `--colorize_arms` when recording Grasp videos to render the left arm/gripper blue and the right arm/gripper yellow. TwoTarget visual objects are also colored blue/yellow in the task config.
- Added `scripts/reinforcement_learning/rl_games/evaluate_grasp_checkpoint.py` to evaluate Grasp checkpoints with object/TCP metrics instead of relying only on video. It reports the bottle root position, left/right TCP positions, TCP-to-target distances, TCP-to-object-center distances, near-target ratios, action rate, max action, and joint velocity. Use it after any Grasp training run before deciding whether to continue training or change rewards.
- Current Easy Grasp video observation: the TCPs still did not visually reach the middle bottle. Treat this as a failed training result until `evaluate_grasp_checkpoint.py` confirms otherwise; do not continue the same setup blindly by only adding iterations.
- Current TwoTarget Grasp target definition has been changed from the upper point `(0, 0, 0.20)` above each target object to the bottle/target object center `(0, 0, 0.0)`. The intended visual check is now whether the fingertip-midpoint TCP coordinate axes coincide with the corresponding object-center target axes.
- RL-Games `play.py` now also accepts playback-only debug coordinate axes: `--debug_axes`, `--debug_axis_scale`, and `--debug_grasp_target_offset`. The debug TCP axes use the midpoint of the narrow/wide fingertip links, and Grasp target axes use the object root plus the configured target offset. The default axis scale is small (`0.06`) to avoid obscuring the robot and targets.
- Current best Grasp approach result is the bottle-center TwoTarget Track run `grasp_approach3d_twotarget_bottlecenter_256env_300it_v1`, trained from the updated target definition. The user reported that it stably reaches the bottle-center targets in playback.
- The current good bottle-center checkpoint has been preserved at `saved_weights/grasp_twotarget_bottlecenter_good_20260707/jz_bi_grasp.pth` with matching `params/agent.yaml` and `params/env.yaml`. Use container checkpoint path `/workspace/saved_weights/grasp_twotarget_bottlecenter_good_20260707/jz_bi_grasp.pth` if the project root is mounted as `/workspace`; otherwise use the original RL-Games run checkpoint.
- Current Grasp physics status: the robot is an Isaac Lab `Articulation` running in Isaac Sim/PhysX with collision, joint limits, actuator limits, and PD control. The bottle/target objects are rigid cuboids with collision, mass, friction, restitution, and damping configured. The existing TwoTarget/Track approach tasks keep those objects kinematic and gravity-disabled fixed targets for approach training, while the new `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Dynamic-v0` and `-Dynamic-Play-v0` variants switch both objects to dynamic rigid bodies (`kinematic_enabled=False`, `disable_gravity=False`) for contact validation before true grasp/lift reward training.
- Robot gripper/TCP asset check: `left_arm_link9/right_arm_link9` remain as invisible parent frames for the gripper joints, and their visual/collision geometry has been removed. Because the visible `narrow*/wide*` finger meshes are authored in the old gripper frame, `left_arm_fixed_joint2/right_arm_fixed_joint2` use the previous movable-finger-aligned rotations (`rpy="-1.5708 1.5708 0"` and `rpy="0 -1.5708 1.5708"`), not the intermediate link9-visible `rpy="0 0 0"` mounting. The moving `narrow*/wide*` gripper links carry the visual/collision finger meshes, so rendered/colliding finger geometry follows gripper joint motion. Current generic position TCP uses `left_arm_link7/right_arm_link7` with offsets left `(0.0385, 0.23772, 0.0)` and right `(0.0385, -0.23772, 0.0)`, moved `0.03m` farther toward the corresponding bottle from the prior offsets. Semantic orientation still comes from corrected `link9`; SurfacePregraspV3 inner-fingertip midpoint rewards are separate and unchanged.
- Gripper collision investigation: `/mnt/data2/ybd/robot_simulator/robot_urdf.xacro` is useful as an original standalone gripper topology reference. It confirms STL collision geometry exists for `narrow*/wide*` and for `narrow_loop/wide_loop`, but it does not add Gazebo/PhysX material/contact properties. Do not directly merge the xacro into Isaac Lab, because its distal and loop links are modeled as extra revolute joints and would change the current 18-D action contract. The active robot spawn config explicitly sets collision props (`collision_enabled=True`, `contact_offset=0.005`, `rest_offset=0.0`) and rebuilds USD with `make_instanceable=False`. The temporary simplified gripper box colliders and red playback overlay were removed because their volume did not match the finger shape. The active movable-finger topology relies on the original `narrow*/wide*` collision STL meshes attached to the moving finger links. `link9` remains collision-free; any link9 collision would be fixed to the wrist and would not follow gripper opening/closing.
- TwoTarget Grasp gripper action is no longer locked to zero scale. Fixed/Approach/TwoTarget Grasp variants now use default-pose-offset gripper joint-position actions, so zero action preserves the open reset state and nonzero gripper actions can move the gripper joints and the visible moving finger meshes. The current movable-finger validation video is `jz_isaac_lab/logs/debug_reach_env/gripper_movable_fingers_sweep_v1/videos/rl-video-step-0.mp4`; it was recorded after forcing USD rebuild and confirmed gripper joints moved from `[-1.2, 1.2]` toward about `[-0.4, 0.4]` and back open. The earlier `gripper_tcp_default_restored_v1`, `gripper_sweep_restored_v1`, and red collision-box overlay videos should not be used as acceptance evidence for the current STL-collision topology.
- Dynamic TwoTarget validation: `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Dynamic-v0` keeps the 18-D action layout and 66-D observation layout compatible with the bottle-center checkpoint, while making both left/right target cuboids dynamic. A short `print_grasp_object_sanity` run confirmed nonzero object linear/angular velocities. Playback with the existing bottle-center checkpoint and TCP/target axes was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 15:13; note this `videos/play` file is overwritten by subsequent playback runs.
- Minimal closure/contact curriculum: `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-GraspClose-v0` and its Play variant preserve the 66-D policy observation and 18-D action layout, use two dynamic `0.03m` radius by `0.12m` height cylinders, and filter real contact forces from each distal narrow/wide finger link to its assigned cylinder. The only new active reward behavior is early-close `-0.3`, near-and-slow close `+0.8`, bilateral fingertip contact `+2.0`, and stable-contact dwell `+1.0`; there is no lift reward and no contact observation.
- Closure/contact run `grasp_close_contact_256env_300it_v1` fine-tuned the preserved bottle-center approach checkpoint for 300 additional epochs on GPU 4 with 256 environments. Use `nn/last_jz_bi_grasp_ep_600_rew_-1.4263284.pth` for evaluation rather than the early `jz_bi_grasp.pth`. A 16-env deterministic evaluation produced last-50 TCP-to-cylinder distances about `0.0587m` left and `0.0623m` right with zero dual 5cm hit ratio; the right cylinder moved under contact, but playback showed incomplete bilateral closure and pushing rather than a stable grasp. The unforced policy video is `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_close_contact_256env_300it_v1/videos/play/rl-video-step-0.mp4`.
- Gripper closure blocker found after the closure run: the open defaults are narrow/wide `[-1.2, 1.2]`, true closed limits are about `[-0.05, -0.05]`, but the default-offset action scale is `1.15` and RL actions are clipped to `[-1, 1]`. The strongest close command therefore reaches only about `[-0.05, +0.05]`; the wide joint cannot reach its negative closed limit. A forced-close diagnostic showed all four filtered fingertip contact forces at zero through 180 steps and no cylinder displacement. Do not retrain this task until the gripper action mapping is changed and a forced-close contact test passes.
- `play.py` now supports `--debug_colliders`, `--print_gripper_diagnostics`, and `--diagnostic_every`. The collider/forced-close video is `artifacts/grasp_close_contact_256env_300it_v1/forced_close_colliders.mp4`; the prior unforced policy video is preserved as `artifacts/grasp_close_contact_256env_300it_v1/policy_unforced.mp4`.
- Grasp semantic TCP and bottle-target frames are now symmetric and right-handed. On the robot's right side, blue Z is world-up, red X points outward (`-world Y`), and green Y points forward (`+world X`); the left frame is the 180-degree rotation around world Z, so X points `+world Y` and Y points `-world X`. Target `wxyz` quaternions are left `(0.70710678, 0, 0, 0.70710678)` and right `(0.70710678, 0, 0, -0.70710678)`. Side-specific offsets correct the asymmetric URDF `link9` frames without changing gripper geometry. At reset, corrected TCP quaternions numerically match the target quaternions. Validation video: `jz_isaac_lab/logs/debug_reach_env/tcp_symmetric_right_xyz_v1/videos/rl-video-step-0.mp4`.
- Open-gripper 6DOF approach run: `grasp_open6d_axes_256env_300it_v1` resumed the good bottle-center checkpoint at epoch 300 and completed 300 additional epochs on physical GPU 0 with 256 environments. The task keeps both cylinders fixed, locks both grippers open, and adds the approved near-gated TCP-axis alignment reward at `+0.5` per side without changing the 66-D observation or 18-D action layouts. Deterministic 64-env evaluation shows this is not a successful bilateral 6DOF policy: the best checkpoint's last-50 TCP distances are about `0.0223m` left and `0.0388m` right, but left/right TCP-Z alignment dots are `-0.750/0.941` and TCP-Y aim dots are `-0.003/0.799`. The right side learned the intended pose while the left side remained inverted. Playback: `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_open6d_axes_256env_300it_v1/videos/play/rl-video-step-0.mp4`. Do not continue this exact reward setup or use this run for closure training until the left-side orientation signal is corrected and revalidated.
- Fingertip contact-point calibration: the old narrow3 `(0.0214, 0.0054, 0)` and wide3 `(0.0214, -0.0053, 0)` offsets lie on the outer faces of their collision STL meshes and must not be used for grasp-centering rewards. Inward ray/triangle intersections produced accepted inner-surface points narrow3 `(0.0214, -0.01704202, 0)` and wide3 `(0.0214, 0.01789719, 0)` in each link's local frame. They are shared by the active SurfacePregrasp rewards and the 3mm red/green debug/playback markers. Point-placement validation video: `jz_isaac_lab/logs/debug_reach_env/fingertip_inner_surface_points_right_closeup_v4/videos/rl-video-step-0.mp4`.
- SurfacePregrasp run: `grasp_surface_pregrasp_256env_300it_v1` completed 300 additional epochs from the good bottle-center checkpoint on physical GPU 0. Its task uses the calibrated inner collision-surface points in active rewards, keeps fixed cylinders and zero-scaled open grippers, removes TCP-center rewards, and preserves the 66-D/18-D contract. This run failed geometric acceptance. The safer epoch-550 checkpoint has last-50 left/right pair gaps `0.0372/0.0316m` versus the `0.015m` target, height differences `0.1413/0.1097m`, Z dots `0.179/0.613`, and early-contact ratios `0/0.0328`. Epoch 600 is worse despite a right gap of `0.0117m`: right contact ratio is `0.8019` and approach dot is `-0.221`. Do not use epoch 600 or advance this run to closure training. The multiplicative centering term effectively vanished under large initial height error; any next reward revision requires user approval. Epoch-550 playback: `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_surface_pregrasp_256env_300it_v1/videos/play/rl-video-step-0.mp4`.
- SurfacePregraspV2 run `grasp_surface_pregrasp_v2_256env_300it_v1` trained from scratch with the 96-D observation contract and reached returns `40.31/45.52/57.58` at epochs `100/200/300`, but user playback showed it no longer reached the targets. Treat this as reward hacking, not convergence: V2 has no dense approach reward, its Z/approach/level rewards can be earned far away, and contact over `1N` terminates the episode. Do not extend or promote this checkpoint.
- User approved the separate `SurfacePregraspV3` revision. V3 preserves V1/V2 and keeps 96-D observations/18-D actions, but adds dense distance, broad tracking, and signed progress from the calibrated inner-fingertip midpoint to each cylinder center. Pose terms use a continuous `0.15m` proximity gate; fine clearance/centering/level terms use a `0.08m` gate. V3 retains a reduced `-1.0` early-contact penalty but disables contact termination. Validate with a 64-env 20-epoch smoke before any 100/300-epoch run; accept based on distance/hit/contact/geometry metrics rather than return alone.
- On 2026-07-13, V3 dense distance/tracking/progress was switched from the open-gripper inner-fingertip midpoint to the generic position TCP after that TCP was moved `0.03m` farther toward each bottle. Fine geometry, distance gates, and contact metrics still use the calibrated narrow3/wide3 inner-surface points. Existing V3 checkpoints predate this reward/observation reference change and should not be continued as if the contract were unchanged.
- User approved `SurfacePregraspV4` as the next independent task. It keeps the 96-D/18-D contract and fixed open-gripper cylinders, but guides the updated generic TCP to a `0.03m` radial stand-off from each cylinder Z axis while independently aligning to bottle-center height. It adds contact-aware signed radial progress, near-target radial and total speed braking, earlier pose/fine gates (`0.30m/0.15m`), `-3.0` instantaneous and up to `-2.0` persistent contact penalties, and a `+2.0` stable no-contact pregrasp dwell term. Contact still does not terminate the episode. Train from scratch and accept using radial/height/speed/contact metrics, not total return.
- V4 512-env/100-epoch probe reached the right radial target (`-0.0073m` last-50 error) with low radial speed, but failed height/stability/table acceptance: right height error was `-0.0323m`, relative speed `0.123m/s`, cylinder contact ratio `17.3%`, max action about `5.36`, and video showed lower-gripper table penetration. Left radial/height errors remained about `0.218/0.106m`.
- V4.1 was restored as a clean independent variant after user approval, while SurfacePregraspV4 remains unchanged. The current V4.1 uses V4's unclipped action behavior and keeps the `+0.03m` height target, direct radial/height errors, stronger braking, and a 20-step stable hold; corrected Z/approach-axis pose weights are raised to `1.5/1.0`. It adds no V4.1-specific table sensors or table-related reward terms; V4's pre-existing generic-TCP clearance remains inherited unchanged. Older V4.1 checkpoints predate this current contract and must not be loaded into the new run.
- Restored V4.1 runtime smoke `grasp_surface_pregrasp_v41_notable_pose_64env_5it_smoke` completed `5/5` epochs on physical GPU 4. Isaac Lab confirmed the `96-D/18-D` interface and all 62 active reward terms, including pose weights `1.5/1.0`, direct radial/height penalties, and strengthened speed terms. The `-inf` checkpoint return is expected because five PPO epochs finish before the first 8-second environment episode terminates; use the smoke only as configuration/runtime validation.
- The clipped-action V4.1 probe `grasp_surface_pregrasp_v41_notable_pose_512env_100it_probe_v1` failed: deterministic last-50 left/right radial errors were `0.3838/0.2455m`, while actions remained saturated at exactly `1.0`. Its checkpoint and video are rejected references only. By user approval, V4.1 action clipping is now removed while all non-table V4.1 reward changes and stronger pose shaping remain.
- Unclipped replacement smoke `grasp_surface_pregrasp_v41_unclipped_pose_64env_5it_smoke` completed `5/5` epochs on physical GPU 4 with the expected 62 reward terms. Use a fresh unclipped run; never continue from the rejected clipped checkpoint.
- V4.1 unclipped probe `grasp_surface_pregrasp_v41_unclipped_pose_512env_100it_probe_v2` recovered the right-hand target but did not stop: last-50 right radial distance/error were `0.0207/-0.0093m`, relative speed was `0.2703m/s`, and action rate was `1.3338`; left radial error remained `0.4961m`. Fingertip-only contact sensors reported zero despite video-observed gripper/bottle collision.
- User approved independent V4.2. It moves the radial stop target to `0.05m`, progressively limits relative speed to `0.15/0.08/0.03m/s` as radial error enters `0.10/0.05/0.02m`, penalizes continued inward motion after overshoot at `-10.0`, and requires at most `0.03m/s` for stable hold. No table-specific shaping is added.
- V4.2 smoke `grasp_surface_pregrasp_v42_braking_64env_5it_smoke` completed `5/5` epochs on physical GPU 4. Isaac Lab assembled the expected `96-D/18-D` interface and 66 active reward terms, including the four new per-side braking/overshoot terms.
- V4.2 probe `grasp_surface_pregrasp_v42_braking_512env_100it_probe_v1` reached epoch 100 at return `22.6097` but learned a conservative far-stop local optimum. Deterministic last-50 right radial distance/error were `0.2007/0.1507m` against the `0.05m` target; relative speed fell to `0.0346m/s` with zero reported fingertip contact. Left radial error remained `0.4537m`. Do not continue this checkpoint without revising the approach/braking balance.
- User approved independent V4.3 from the unclipped V4.1 baseline. It replaces all squared/gated speed penalties with signed linear radial-velocity tracking using a `5mm` deadband, gain `1.5`, inward/outward caps `0.15/0.08m/s`, initial weight `-1.0`, and near-target tangential weight `-0.5`. The stop radius remains `0.05m`, inward motion after overshoot is `-5.0`, and stable hold is `+3.0` at no more than `0.03m/s`. No table-specific shaping is added.
- V4.3 smoke `grasp_surface_pregrasp_v43_linear_velocity_64env_5it_smoke` completed `5/5` epochs on physical GPU 4. Runtime inspection confirmed 64 active terms: both old per-side squared speed terms were absent and all six new per-side linear velocity/tangential/overshoot terms were active.
- V4.3 probe `grasp_surface_pregrasp_v43_linear_velocity_512env_100it_probe_v1` reached epoch 100 at return `34.1566`. Linear tracking reduced right last-50 relative speed to `0.0298m/s`, but the TCP overshot the `0.05m` target to radial distance/error `0.0227/-0.0273m` and height error `-0.0345m`; right semantic Z/inward-axis dots were `0.908/0.904`, while fingertip-geometry approach dot remained only `0.123`. Left radial error remained `0.4999m`. This is improved braking but not accepted due inward/height overshoot and likely collision.
- User approved independent V4.4. It keeps V4.3 linear braking but adds full six-link finger and link9/10 palm contact coverage, a static `0.05m` radial safety barrier saturating at `-3.0` by the `0.03m` bottle radius, `3x` inside-target velocity recovery, full/palm instantaneous and persistent collision penalties, a tighter `±0.005m` safe hold, and stronger height shaping. No table-specific shaping is added.
- V4.4 contact-sensor implementation was corrected after a regex multi-body filtered sensor produced PhysX filter-count errors. The accepted implementation uses one filtered sensor per link and aggregates six finger plus two palm sensors per side. Fresh isolated-scene and V4.3-checkpoint smokes passed on physical GPU 4 with 68 active reward terms and no filter-count errors. Because the source checkpoint is epoch 100, a 25-epoch fine-tune must use `--max_iterations 125`.
- Corrected TCP-axis validation video: after changing the synthetic TCP to the link7 distal end, the original bottle-center checkpoint action video with forced gripper close and TCP/target axes was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:58; note this `videos/play` file is overwritten by subsequent playback runs.
- RL-Games `play.py` now has playback-only gripper overrides: `--force_gripper_close`, `--gripper_close_value`, and `--gripper_close_ramp_steps`. These keep checkpoint arm actions but overwrite action channels 14-17 as `(+close, -close, +close, -close)` for left/right grippers. The bottle-center checkpoint action video with forced gripper close was recorded at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:41; this path is overwritten by later play videos.
- `play.py --start_gripper_closed` is the separate playback reset override for a visibly closed default gripper pose. It sets narrow joints to `-0.0501` and wide joints to `-0.0499`, just inside their respective URDF limits, before constructing the environment; it does not change the training task's open reset pose or the 18-D action contract. A 239-frame checkpoint playback with this override and TCP/target axes was recorded on 2026-07-10 at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4`.
- Rebuild the JZ bimanual USD before the next visual validation. The source and resolved URDFs have the corrected movable-finger asset, but older USD cache files can still be loaded unless `JZLAB_ALLOW_STALE_USD_CACHE=0` or a forced conversion is used.
- Current Reach tuning rationale and parameter plan are recorded in `harness/tasks/reach_tuning_log.md`.

## Known Working Baseline
- Docker mount target for the RL project: `/workspace/jz_isaac_lab`.
- Host RL project path: `/home/cqy/workspace/middle_platform/robot_simulator/jz_isaac_lab`.
- Initial task: `Isaac-Reach-JZ-Bi-v0`.
- Experimental task: `Isaac-Reach-JZ-Bi-Easy-v0`.
- Control-alignment experimental task: `Isaac-Reach-JZ-Bi-Control-v0`.
- Short-term position-only task: `Isaac-Reach-JZ-Bi-PosOnly-v0`.
- Short-term smooth position-only task: `Isaac-Reach-JZ-Bi-PosOnly-Smooth-v0`.
- Short-term Grasp 3D approach task: `Isaac-Grasp-JZ-Bi-Approach-3D-v0`.
- Easier Grasp 3D approach sanity task: `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-v0`.
- Current best Grasp approach task: `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Track-v0` with bottle-center targets.
- Preserved good Grasp approach checkpoint: `/workspace/saved_weights/grasp_twotarget_bottlecenter_good_20260707/jz_bi_grasp.pth`.
- Training can run headless with `+agent.params.config.torch_compile=False`.
- Runtime context: `harness/context/docker_isaac_lab.md`.
- Server migration guide: `harness/context/server_migration.md`.
- Reach task anatomy: `harness/context/reach_task_anatomy.md`.
- RL task contract: `harness/contracts/rl_tasks.md`.
- Reach training task guide: `harness/tasks/reach_training.md`.
- Reach tuning log: `harness/tasks/reach_tuning_log.md`.
- Reach environment bring-up plan: `harness/tasks/reach_environment_bringup_plan.md`.
- Reach PosOnly handoff explanation: `harness/tasks/reach_posonly_handoff.md`.
- Grasp IK-gated RL plan: `harness/tasks/grasp_ik_rl_plan.md`.
- JZ/OpenArm control-alignment context: `harness/context/jz_openarm_alignment.md`.
- Latest baseline result: `harness/tasks/reach_training.md`.
- Best available demo checkpoint: `/workspace/jz_isaac_lab/legacy_checkpoints/legacy_handoff/jz_bi_reach.pth`.

## Long-Term Memory Rule
This file is long-term memory. Chat history is not long-term memory. Update this file whenever repository structure, configuration, interfaces, runtime behavior, or project phase changes.
