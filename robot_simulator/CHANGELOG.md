# Changelog

- Added `learning_records/` with a Q&A-based grasp RL debugging record covering position-versus-speed rewards, staged braking, TCP/TF semantics, contact blind spots, action clipping, table shaping, SurfacePregrasp V1-V4.2 outcomes, evaluation metrics, and Hydra/Docker runtime pitfalls.

## 2026-07-13
- Moved the shared synthetic position TCP `0.03m` farther toward each bottle: the left `left_arm_link7` local offset changed from `(0.0385, 0.20772, 0)` to `(0.0385, 0.23772, 0)`, and the right offset changed from `(0.0385, -0.20772, 0)` to `(0.0385, -0.23772, 0)`. This changes the generic TCP position only; semantic orientation corrections, URDF frames, calibrated inner-fingertip points, and SurfacePregraspV3 inner-midpoint rewards are unchanged.
- Switched SurfacePregraspV3 dense distance, Gaussian tracking, and signed progress shaping from the open-gripper inner-fingertip midpoint to the updated generic position TCP. Fine clearance/centering geometry and contact handling remain based on the calibrated distal-finger inner-surface points. V1/V2 are unchanged.
- Added user-approved SurfacePregraspV4 train/play tasks. V4 replaces V3's 3D center-distance shaping with generic-TCP radial tracking to the cylinder Z axis (`0.03m` target radius), independent bottle-center height tracking, contact-aware radial progress, and near-target radial/total speed braking. It advances pose/fine gates to `0.30m/0.15m`, uses instantaneous/persistent contact penalties `-3.0/-2.0`, keeps episodes alive after contact, and rewards a no-contact stable radial/height hold. The evaluator now reports TCP axis radial distance/error, height error, radial speed, and relative speed.
- Added SurfacePregraspV4.1 train/play tasks and a dedicated RL-Games config with policy/environment action clipping enabled at `[-1,1]`. V4.1 retains V4 radial behavior, targets TCP height `+0.03m` above bottle center, adds direct radial/height absolute-error penalties, strengthens near-target braking, and requires a 20-step low-speed stable hold.
- Added V4.1 full-gripper-to-table ContactSensors, `-5.0` instantaneous and persistent table-contact penalties, and a `-40.0` calibrated-inner-fingertip table-clearance penalty with `0.02m` margin. Stable-pregrasp reward now rejects table contact. The evaluator reports table force, contact ratio, and clearance violation for V4.1.
- Removed all V4.1 table-specific sensors, rewards, stable-hold conditions, and evaluator fields after the resulting policy learned to move away from the table. V4.1 now contains only action clipping, direct radial/height shaping, raised height target, stronger braking, and near-target action-rate changes; do not reintroduce table-related shaping without explicit approval.
- Fully retired and removed the V4.1 task registrations, environment subclasses, direct-error reward helpers, and dedicated clipped-action agent config at user direction. The active task is restored to unchanged SurfacePregraspV4. Existing V4.1 log directories/checkpoints are retained only as rejected experiment history.
- Restored V4.1 as an independent variant by user approval. It keeps the `+0.03m` height target, direct radial/height errors, stronger near-target braking, and a 20-step stable hold; corrected Z/approach pose weights are increased to `1.5/1.0`. No V4.1-specific table sensors or table rewards were restored; V4's inherited generic-TCP clearance remains unchanged. Action clipping was subsequently removed after the clipped probe failed.
- Validated restored V4.1 with a `64-env x 5-epoch` Docker smoke on physical GPU 4. The task entered RL-Games, assembled the expected 96-D observation, 18-D action, and 62 reward terms, completed training, and saved a smoke checkpoint.
- Removed V4.1 policy/environment action clipping after the 100-epoch probe saturated at `1.0` and failed to approach either cylinder. V4.1 retains its height, direct-error, braking, stable-hold, and stronger pose changes; V4 remains unchanged.
- Validated the unclipped V4.1 replacement with a fresh `64-env x 5-epoch` smoke on physical GPU 4.
- Added user-approved SurfacePregraspV4.2 train/play tasks. V4.2 moves the radial stop target to `0.05m`, adds progressive relative-speed limits `0.15/0.08/0.03m/s` over the final `0.10/0.05/0.02m`, strongly penalizes inward motion after crossing the target, and tightens stable speed to `0.03m/s`. It adds no table-specific shaping and leaves V4/V4.1 unchanged.
- Validated V4.2 with a fresh `64-env x 5-epoch` Docker smoke on physical GPU 4; all 66 reward terms assembled and the run completed normally.
- Recorded V4.2 epoch-100 probe results: braking reduced right relative speed to `0.0346m/s`, but the policy stopped at `0.2007m` radial distance instead of the `0.05m` target. Marked the checkpoint as a far-stop local optimum rather than an accepted pre-grasp policy.
- Added user-approved SurfacePregraspV4.3 train/play tasks from the unclipped V4.1 baseline. V4.3 replaces squared/gated speed penalties with signed linear radial-velocity tracking, a `5mm` deadband, near-target linear tangential braking, reduced inward-overshoot weight `-5.0`, and stable hold `+3.0`; it retains the `0.05m` stop radius and adds no table-specific shaping.
- Validated V4.3 with a fresh `64-env x 5-epoch` Docker smoke on physical GPU 4. Isaac Lab assembled 64 active terms with the legacy squared speed terms disabled and the new linear terms enabled.
- Recorded V4.3 epoch-100 results: right relative speed reached the `0.03m/s` target, but radial distance overshot to `0.0227m` against the `0.05m` stop radius and height was `0.0345m` low. Marked the run as improved braking but geometrically unaccepted.
- Added user-approved SurfacePregraspV4.4 train/play tasks. V4.4 adds individual filtered sensors for all six finger links plus link9/10 palm bodies against each cylinder, aggregates them for full/palm instantaneous and persistent penalties, adds a normalized static `0.05m` safety barrier, stronger inside-target recovery, tighter safe stable hold, stronger height shaping, and evaluator full-gripper metrics. No table-specific shaping was added.
- Validated the corrected per-link V4.4 sensors with fresh Docker smokes on physical GPU 4. The V4.3 epoch-100 checkpoint loaded successfully into V4.4; continuation must set total `max_iterations` to 125 for 25 additional epochs.

## 2026-07-11
- Added the user-approved `SurfacePregraspV3` train/play tasks without modifying V1/V2. V3 keeps the 96-D observation and 18-D action contracts, adds dense calibrated-inner-fingertip-midpoint distance (`-2.0`), tracking (`+1.0`, `std=0.25m`), and signed progress (`+2.0`) rewards per arm, proximity-gates pose rewards at `0.15m` and fine geometry rewards at `0.08m`, reduces early-contact penalty from `-5.0` to `-1.0`, and removes contact-triggered episode termination for V3 only.
- Recorded `grasp_surface_pregrasp_v2_256env_300it_v1` as a reward-hacking failure: return increased from about `40.31` at epoch 100 to `57.58` at epoch 300 while playback moved farther from the cylinders. Do not extend V2; its dense approach rewards are absent while several pose rewards remain available far from the target.

## 2026-07-10
- Added `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-SurfacePregrasp-v0` and its Play variant. The task preserves the 66-D observation and 18-D action interfaces, fixed cylinders, and open grippers; it disables TCP-to-cylinder-center rewards and instead uses the approved per-side rewards: inner-finger surface clearance `+1.0`, 3D grasp centering `+1.0`, corrected side-specific orientation `+0.5`, and early real contact `-0.5`.
- Trained `grasp_surface_pregrasp_256env_300it_v1` for 300 additional epochs from the good bottle-center checkpoint on physical GPU 0 with 256 environments. Training completed at epoch 600; best training return was about `11.70` near epoch 450, epoch 550 returned `11.29`, and epoch 600 returned `10.77`.
- Added raw SurfacePregrasp checkpoint metrics to `evaluate_grasp_checkpoint.py`: both finger gaps, projected between-finger coordinate, centerline/height errors, corrected Z/approach dots, forces, and early-contact ratio. The run did not pass geometric acceptance. Epoch 550 last-50 left/right pair gaps were about `0.0372/0.0316m`, height differences `0.1413/0.1097m`, Z dots `0.179/0.613`, and early-contact ratios `0/0.0328`. Epoch 600 reduced the right gap to `0.0117m` by colliding: right early-contact ratio rose to `0.8019` and right approach dot became `-0.221`. Do not use epoch 600.
- Added `play.py --debug_fingertip_contact_points` and recorded the evaluation-selected epoch-550 policy with TCP/target axes and reward contact points at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_surface_pregrasp_256env_300it_v1/videos/play/rl-video-step-0.mp4` (`1280x720`, 229 frames, no timeout reset).
- Added `debug_reach_env.py --debug_fingertip_contact_points` to render the four candidate distal-finger contact points during model checks. The original offsets were found on the outer collision-mesh faces. Ray casting inward through the actual STL triangles produced inner-face candidates at narrow3 `(0.0214, -0.01704202, 0)` and wide3 `(0.0214, 0.01789719, 0)` in link-local coordinates, about `22.44mm` and `23.20mm` inward from the old points. Narrow3 markers are red, wide3 markers are green, and their radius is `3mm`. They were first validated diagnostically at `jz_isaac_lab/logs/debug_reach_env/fingertip_inner_surface_points_right_closeup_v4/videos/rl-video-step-0.mp4`, then promoted to shared SurfacePregrasp reward constants after user approval.
- Added the approved open-gripper 6DOF approach tasks `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Open6D-v0` and `-Open6D-Play-v0`. They preserve the 66-D observation and 18-D action interfaces, keep both cylinders fixed and both grippers open, and add a near-gated semantic TCP-axis reward (`TCP Z` aligned with cylinder Z and `TCP Y` aimed toward the cylinder center) at weight `+0.5` per arm.
- Fine-tuned the bottle-center approach checkpoint for 300 additional epochs on physical GPU 0 with 256 environments in `grasp_open6d_axes_256env_300it_v1`. The run completed at epoch 600; training best return reached about `31.00` and final return was about `30.28`.
- Extended `evaluate_grasp_checkpoint.py` with semantic TCP orientation diagnostics. A deterministic 64-env evaluation found that the Open6D run preserved approach accuracy but learned only the right-side orientation: the best checkpoint's last-50 distances were about `0.0223m` left and `0.0388m` right, while left/right TCP-Z alignment dots were about `-0.750/0.941` and TCP-Y aim dots were about `-0.003/0.799`. Treat this run as a failed bilateral 6DOF result; do not use it as the base for closure training without correcting the left-side orientation objective or learning signal.
- Recorded the Open6D best-checkpoint playback with TCP/target axes at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_open6d_axes_256env_300it_v1/videos/play/rl-video-step-0.mp4` (`1280x720`, 299 frames).
- Standardized Grasp TCP and bottle-target axes as symmetric right-handed frames. On the robot's right side, blue Z is world-up, red X points outward (`-world Y`), and green Y points forward (`+world X`); the left frame is rotated 180 degrees around world Z. Shared `wxyz` targets are left `(0.70710678, 0, 0, 0.70710678)` and right `(0.70710678, 0, 0, -0.70710678)`. Cylinder rigid-body orientation is unchanged.
- Added side-specific fixed quaternion corrections from the asymmetric URDF `link9` frames to semantic TCP frames. Updated `play.py` and `debug_reach_env.py` to display corrected TCP axes and matching bottle-target axes. Numeric reset validation matched both target quaternions; video is at `jz_isaac_lab/logs/debug_reach_env/tcp_symmetric_right_xyz_v1/videos/rl-video-step-0.mp4`.
- Added playback diagnostics `--debug_colliders`, `--print_gripper_diagnostics`, and `--diagnostic_every` to show all PhysX collision shapes and print gripper joints, four filtered fingertip contact forces, and object positions.
- Diagnosed the failed closure run: with open defaults `[-1.2, 1.2]`, action scale `1.15`, and clipped actions in `[-1, 1]`, the narrow joint can reach `-0.05` but the wide joint can only reach `+0.05`; its true closed limit is `-0.05`. A forced 90-step close reached about `[-0.05, +0.05]`, all four filtered contact forces remained zero, and neither cylinder moved. The current gripper action mapping cannot command the true closed pose and must be corrected before retraining.
- Recorded the collider/forced-close diagnostic at `artifacts/grasp_close_contact_256env_300it_v1/forced_close_colliders.mp4`; preserved the original unforced policy playback at `artifacts/grasp_close_contact_256env_300it_v1/policy_unforced.mp4`.
- Added the minimal closure curriculum tasks `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-GraspClose-v0` and `-GraspClose-Play-v0`. They keep the 66-D observation and 18-D action interfaces, replace the targets with dynamic cylinders, add filtered contact sensors on the four distal finger links, and intentionally omit lift rewards and contact observations.
- Added the approved closure rewards: early-close penalty `-0.3`, near-and-slow close reward `+0.8`, real bilateral fingertip contact reward `+2.0`, and stable-contact dwell reward `+1.0` per arm.
- Fine-tuned the bottle-center approach checkpoint for 300 additional epochs with 256 environments in `grasp_close_contact_256env_300it_v1`. The final epoch-600 checkpoint improved last-50 TCP distances to about `0.0587m` left and `0.0623m` right, but the 5cm dual-target ratio remained zero and playback showed incomplete closure plus right-cylinder pushing. This run is a contact-learning probe, not a successful grasp policy.
- Recorded the unforced epoch-600 policy playback with TCP/target axes at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_close_contact_256env_300it_v1/videos/play/rl-video-step-0.mp4` (`1280x720`, 299 frames).
- Advanced the synthetic TCP from the `link7`-to-`link8` fixed joint toward each gripper by `0.10m` along the displayed TCP blue axis. Since orientation follows `link9`, the equivalent `link7` local offsets are left `(0.0385, 0.20772, 0.0)` and right `(0.0385, -0.20772, 0.0)`. Recorded a static right-wrist axis check at `jz_isaac_lab/logs/debug_reach_env/tcp_right_wrist_link7_tcp_blue_plus_010_v1/videos/rl-video-step-0.mp4`.
- Added playback-only `play.py --start_gripper_closed`. It changes both gripper reset/default joint positions to the legal closed pose before environment construction while retaining the checkpoint's arm actions and the 18-D action layout. The closed values are narrowly inside the URDF limits: narrow `-0.0501`, wide `-0.0499`.
- Recorded a 239-frame default-closed TwoTarget Track checkpoint playback with TCP/target axes at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4`.

## 2026-07-09
- Added playback-only `play.py --force_gripper_close` with `--gripper_close_value` and `--gripper_close_ramp_steps`, so checkpoint arm actions can be replayed while forcing the gripper action channels to close for visual validation.
- Recorded checkpoint action playback with forced gripper close at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:41.
- Moved the synthetic TCP position used by Grasp/Reach MDP helpers and playback/debug axes to the distal end of `left_arm_link7/right_arm_link7`, using local offsets `(0.0385, 0.10772, 0.0)` and `(0.0385, -0.10772, 0.0)`.
- Recorded link7-end TCP playback with forced gripper close at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:58.
- Recorded pure root-finger-midpoint TCP playback with forced gripper close at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:51.
- Recorded corrected TCP-axis checkpoint action playback at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 16:25.
- Added `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Dynamic-v0` and `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Dynamic-Play-v0`. They inherit the TwoTarget Track observation/action contract but switch both target cuboids to dynamic rigid bodies for real contact validation.
- Extended `debug_reach_env.py --mode print_grasp_object_sanity` to print left/right object world pose and linear/angular velocity. A Dynamic smoke test kept action_dim `18`, observation dim `66`, and showed nonzero object velocities.
- Recorded Dynamic checkpoint playback with TCP/target axes at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4` on 2026-07-09 15:13; this play output path is overwritten by later playback runs.
- Restored `left_arm_fixed_joint2` and `right_arm_fixed_joint2` to the previous movable-finger-aligned gripper rotations (`rpy="-1.5708 1.5708 0"` and `rpy="0 -1.5708 1.5708"`), because the visible `narrow*/wide*` finger meshes are authored in that old gripper frame.
- Switched gripper visual/collision geometry to the movable `narrow*/wide*` links and removed visual/collision geometry from `left_arm_link9/right_arm_link9`. `link9` remains as the invisible parent frame for the gripper joints, while the visible finger meshes now follow gripper joint motion.
- Changed Grasp fixed/approach/TwoTarget gripper actions to default-pose-offset joint-position actions with nonzero scale, so zero action preserves the open reset pose and nonzero actions can drive the gripper joints numerically.
- Added `gripper_sweep_video` mode to `debug_reach_env.py` for model-validation videos that drive only the gripper action channels and print gripper joint values during the sweep.
- The previously recorded `gripper_tcp_default_restored_v1` and `gripper_sweep_restored_v1` videos were from an incorrect mixed-geometry variant with both fixed `link9` meshes and moving `narrow*/wide*` meshes, so they should not be used as acceptance evidence for the current movable-finger asset.
- Rebuilt the JZ bimanual USD and recorded the current movable-finger gripper sweep video at `jz_isaac_lab/logs/debug_reach_env/gripper_movable_fingers_sweep_v1/videos/rl-video-step-0.mp4`.
- Rebuilt the JZ bimanual USD again and recorded close TwoTarget checkpoint playback with arm colors and debug axes at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_approach3d_twotarget_bottlecenter_256env_300it_v1/videos/play/rl-video-step-0.mp4`.
- Inspected `/mnt/data2/ybd/robot_simulator/robot_urdf.xacro` and `/mnt/data2/ybd/robot_simulator/robot.xacro`: the former is a standalone gripper geometry/topology reference with `narrow*/wide*` and `narrow_loop/wide_loop` collision meshes; the latter only includes that macro. The xacro does not provide extra PhysX/Gazebo contact properties and should not be directly merged because it would add gripper DOFs.
- Added explicit robot collision properties to the JZ bimanual spawn config (`collision_enabled=True`, `contact_offset=0.005`, `rest_offset=0.0`) and changed forced URDF conversion to `make_instanceable=False` so generated USD configuration carries collision settings for the movable-finger robot asset.
- Removed the temporary simplified gripper box colliders and the `play.py --debug_gripper_collision_boxes` red overlay. The active movable-finger topology now relies on the original `narrow*/wide*` collision STL meshes on the moving finger links, which better match the visible gripper shape.
- Rebuild the JZ bimanual USD before the next visual check; the source and resolved URDFs are corrected, but any older USD cache may still reflect a previous asset variant unless `JZLAB_ALLOW_STALE_USD_CACHE=0` or a forced conversion is used.

## 2026-07-08
- Updated the active TwoTarget Grasp approach target definition so each TCP tracks the corresponding bottle/target object center instead of the point `0.20m` above it. The TwoTarget target offset is now `(0.0, 0.0, 0.0)`.
- Added playback debug coordinate axes to RL-Games `play.py` via `--debug_axes`, `--debug_axis_scale`, and `--debug_grasp_target_offset`. The TCP marker uses the midpoint of the two fingertip links; the target marker uses the object center plus the configured target offset. The default debug axis scale is now `0.06`.
- Trained/validated a bottle-center TwoTarget Track approach run that can stably reach the bottle-center targets in playback, using `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-Track-v0`.
- Preserved the current good bottle-center checkpoint at `saved_weights/grasp_twotarget_bottlecenter_good_20260707/jz_bi_grasp.pth` with its `params/agent.yaml` and `params/env.yaml`.
- Clarified the current physics state: the robot is an Isaac Lab articulation running on Isaac Sim/PhysX; the target bottles are rigid cuboids with collision, mass, friction, and damping configured, but the current TwoTarget approach task makes them kinematic and gravity-disabled fixed targets. Full grasp training must later switch the object to dynamic behavior before contact/lift learning.
- Updated the robot URDF to use the available `left_arm_link9.STL` and `right_arm_link9.STL` meshes for `left_arm_link9` and `right_arm_link9` visual/collision geometry instead of the missing `gripper_base_link.STL`; this intermediate link9-visible topology was superseded on 2026-07-09 by the movable-finger topology above.
- Removed visual/collision mesh references from the `narrow*/wide*` gripper links in the intermediate link9-visible topology. This was later superseded on 2026-07-09: `link9` is now an invisible parent frame and visual/collision finger geometry is on the movable `narrow*/wide*` links.

## 2026-07-06
- Validated the legacy Grasp and Drawer task registrations in the active Docker-mounted `jz_isaac_lab` project on GPU 4. `Isaac-Grasp-JZ-Bi-v0` initializes with action shape `18`, observation shape `69`, four action terms, and 31 reward terms. `Isaac-Open-Drawer-JZ-Bi-v0` initializes with action shape `16`, observation shape `72`, four action terms, and 15 reward terms.
- Recorded Grasp/Drawer zero-action drift findings: both tasks still use `JointPositionToLimitsActionCfg` arm actions, so zero action is mapped to joint-limit midpoint targets rather than holding the reset pose. Grasp and Drawer short zero-action MP4s were saved under `jz_isaac_lab/logs/debug_reach_env/`.
- Ran as-built RL-Games smoke training for Grasp and Drawer on GPU 4 with 32 envs for 20 iterations. Grasp saved a checkpoint with reward `6.2294226`; Drawer saved a checkpoint with reward `-inf` and warned that no environment terminated before max epochs, so Drawer needs termination/evaluation checks before longer training.
- Recorded a headless playback video for the as-built Grasp smoke checkpoint at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_20it_smoke/videos/play/rl-video-step-0.mp4`.
- Completed as-built Grasp comparison training `grasp_asbuilt_32env_2000it_v1` on GPU 4 with 32 envs. Final reward was `16.711939`, best saved checkpoint is `jz_bi_grasp.pth` with highest observed reward about `17.25`, and a headless playback video was saved at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_asbuilt_32env_2000it_v1/videos/play/rl-video-step-0.mp4`.
- Added fixed-start Grasp task registrations `Isaac-Grasp-JZ-Bi-Fixed-v0` and `Isaac-Grasp-JZ-Bi-Fixed-Play-v0`. The fixed task disables robot gravity, fixes robot/object reset randomization, uses default-pose-offset arm actions with scale `0.20`, and adds posture/limit penalties.
- Validated fixed-start Grasp zero-action stability: arm drift is near zero over 120 steps, while body joint drift remains about `0.025 rad`.
- Completed fixed-start Grasp short training `grasp_fixed_32env_300it_v1` on GPU 4 with 32 envs. Final reward was `13.934165`, best checkpoint is `jz_bi_grasp.pth`, and a headless playback video was saved at `jz_isaac_lab/logs/rl_games/jz_bi_grasp/grasp_fixed_32env_300it_v1/videos/play/rl-video-step-0.mp4`.
- Diagnosed why fixed-start Grasp still produced odd posture and failed to reach the visible target: the fixed object center starts about `1.11m` from the left TCP and `1.03m` from the right TCP, and the reward asks both hands to approach the same object center.
- Added Grasp side-target reward helpers and a `print_grasp_object_sanity` debug mode to inspect object/TCP positions, TCP-to-object distance, TCP-to-side-target distance, joint posture deviation, and joint velocity.
- Added approach-only Grasp task registrations `Isaac-Grasp-JZ-Bi-Approach-v0` and `Isaac-Grasp-JZ-Bi-Approach-Play-v0`. This curriculum places a fixed gravity-disabled object in a more reachable pose, trains left/right side-target approach, and disables grasp/lift/contact rewards until approach motion is stable.
- Ran local syntax validation for the new debug/config/reward code with `PYTHONPYCACHEPREFIX=/tmp/jz_pycache python -m py_compile ...`. Docker validation needs to be run from a shell with Docker API access.
- Added `harness/tasks/grasp_ik_rl_plan.md`, defining the staged Grasp strategy: IK sanity first, Approach-only PPO second, weak 6D orientation third, gripper/contact fourth, then small randomization.
- Added `jz_isaac_lab/scripts/tools/debug_grasp_ik.py` to solve standalone IK targets for the Grasp/Approach environment and optionally record a joint-interpolation video from the reset pose to the IK solution. The script passed local syntax validation.
- Fixed `JZGraspApproachEnvCfg` configuration import by adding the missing left/right TCP link constants used by its side-target observation and reward overrides.
- Hardened `debug_grasp_ik.py` after the first container run reached environment setup but failed after reset: added explicit trace markers, standalone IK URDF path fallback, and forced traceback printing.
- Fixed `debug_grasp_ik.py` body-chain handling for standalone IK: solver base chains such as `base_link -> body_link3` use only the body joints present in the chain instead of reshaping all five body joints.
- Extended `debug_grasp_ik.py` video playback with `--hold_steps`, automatic minimum video length, and final simulated TCP-to-target distance printing so IK videos include a settle phase instead of ending at the interpolation target.
- Clarified Grasp IK target interpretation after `grasp_approach_ik_sanity_v2`: final TCPs reached the virtual side targets (`0.009m` left and `0.020m` right), but `side_target_y=0.45m` is visually far from the object center and should be treated only as a transitional target. Updated `debug_grasp_ik.py` to print object-center distances and accept viewer camera overrides; the next target scan should try smaller side offsets before PPO training.
- Added limit-aware multi-start selection to `debug_grasp_ik.py` with `--ik_random_starts` and `--min_joint_margin`. A target is now considered usable only when both IK position errors are under the acceptance threshold and both arms keep the configured normalized joint-limit margin.
- Recorded the current Grasp IK scan result: `side_target_y=0.18`, `target_offset_z=0.15` reaches below `0.05m` position error but still places the right arm on a limit (`joint_margin_ratio=0.0000`), so PPO training should wait for a better target geometry or a non-limit IK solution.
- Rejected the earlier `side_target_y=0.30`, `target_offset_z=0.20` Grasp IK candidate after video validation showed a long right-arm rotation path despite acceptable endpoint error and joint-limit margin.
- Added path-aware gating to `debug_grasp_ik.py`: local IK seeds near the current posture, `--max_joint_delta`, `--max_joint_delta_norm`, printed `joint_delta_max_abs`/`joint_delta_l2`, and a stricter usable flag that requires endpoint error, joint-limit margin, and joint-space path length to pass.
- Clarified the Grasp plan as IK-gated RL rather than IK-driven behavior: IK is only used before training to reject bad target geometry and pathological postures; PPO remains the learning method used in environment rollouts.
- Added PPO-first fixed-scene `Isaac-Grasp-JZ-Bi-Approach-3D-v0` and play variant. It removes IK from the training target path, uses hand-written 3D TCP side targets, disables progress/contact/gripper/lift/orientation rewards, and keeps only 3D TCP tracking plus smoothness, posture, and joint-limit penalties for the next 2000-iteration run.
- Added `--camera_eye` and `--camera_lookat` to RL-Games `play.py` so headless playback videos can use a higher camera angle that captures the full upper body.
- Recorded that `grasp_approach3d_256env_2000it_v1` did not reach the targets reliably in video, so the exact 3D approach v1 configuration should not be continued only by increasing iterations.
- Added easier PPO sanity tasks `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-v0` and play variant. This version targets the middle bottle object root with zero side/height offset, raises arm action scale to `0.25`, adds direct distance penalties, strengthens broad target tracking, and relaxes smoothness/posture/limit penalties.
- Added comparison tasks `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-Smooth-v0` and play variant. They keep the Easy target/reward/PPO setup but strengthen only first-layer smoothness/posture weights for side-by-side training against the original Easy run.
- Added `scripts/reinforcement_learning/rl_games/evaluate_grasp_checkpoint.py` for Grasp checkpoint diagnosis. It prints bottle root, left/right TCP positions, target/object-center distances, near-target ratios, action rate, max action, and joint velocity so failed approach videos can be quantified before reward or target changes.
- Recorded the current Easy Grasp playback observation: the TCPs did not reach the middle bottle visually. The next gate is numeric Grasp checkpoint evaluation; do not continue the same run just by increasing iterations.
- Added `mdp.weighted_joint_vel_l2` and comparison tasks `Isaac-Grasp-JZ-Bi-Approach-3D-Easy-Weighted-v0` / Play. This is the second-layer anti-swing experiment after Easy/Smooth evaluation showed zero near-object ratio and large action changes.
- Added easier two-target curriculum tasks `Isaac-Grasp-JZ-Bi-Approach-3D-TwoTarget-v0` / Play. The scene now has a fixed left target cuboid and a fixed right target cuboid on the table; each TCP tracks the corresponding target's upper point instead of both arms tracking the same middle object center.
- Extended `evaluate_grasp_checkpoint.py` with `--left_object` and `--right_object` so TwoTarget checkpoints can be evaluated against separate target objects.
- Added playback-only arm colorization to `play.py` via `--colorize_arms`, `--left_arm_color`, and `--right_arm_color`. Grasp videos can now render the left arm/gripper blue and the right arm/gripper yellow without changing training.
- Colored the TwoTarget visual target cuboids to match the arm colors: left target blue and right target yellow.

## 2026-07-03
- Added `harness/tasks/reach_environment_bringup_plan.md` defining the environment-first plan for producing a short-term usable Reach result: zero-action validation, joint sweep validation, TCP/target/reward sanity checks, and a conservative 3D position-only Reach task before further training.
- Expanded the Reach environment bring-up plan with a server/headless MP4 debugging workflow, including playback recording commands, video discovery paths, SCP retrieval, naming conventions, and video-first judgment criteria.
- Added `jz_isaac_lab/scripts/tools/debug_reach_env.py` to validate the Reach environment without a learned policy. It supports zero-action video, joint-sweep video, and TCP/command/reward sanity printing.
- Recorded that zero-action drift in `Isaac-Reach-JZ-Bi-v0` may be caused by `JointPositionToLimitsActionCfg(rescale_to_limits=True)` mapping zero action toward joint-limit midpoints, so stable zero-action validation should be repeated with the Control-style action task.
- Corrected the reset-randomization diagnostic to preserve the reset event and set its position range to zero. Re-ran zero-action reasoning: Control action offsets are correct, but the default pose drifts under gravity and becomes stable when robot gravity is disabled.
- Added short-term `Isaac-Reach-JZ-Bi-PosOnly-v0` and `Isaac-Reach-JZ-Bi-PosOnly-Play-v0` tasks for a few-day usable Reach result. The tasks disable robot gravity, use Control-style relative joint-position actions, disable reset randomization, and restrict the workspace curriculum to the nearest target pool.
- Fixed unresolved temporary `SceneEntityCfg` usage in `debug_reach_env.py` and `evaluate_checkpoint.py`; left/right TCP diagnostics and checkpoint evaluation now explicitly resolve body and joint ids before computing metrics.
- Re-ran PosOnly TCP/target/reward sanity after resolving `SceneEntityCfg`; left/right TCPs are now distinct and targets are on the correct sides, with initial errors around `0.20m` left and `0.32m` right.
- Added `Isaac-Reach-JZ-Bi-PosOnly-Smooth-v0` and play variant after the first PosOnly short run reached targets with excessive looping. The Smooth variant lowers action scale and strengthens smoothness penalties.
- Recorded qualitative Smooth PosOnly video improvement: `reach_posonly_smooth_64env_300it_v1` reduces looping and target/action jumping compared with the first PosOnly short run.
- Added `harness/tasks/reach_posonly_handoff.md`, a Chinese handoff document explaining the current environment, task code, action/observation design, reward terms, training commands, evaluation commands, video workflow, and next steps.
- Imported and inspected the legacy handoff `jz_bi_reach.pth` checkpoint. It has 68-D observations and loads with the current original Reach task; a 20-step evaluation on GPU 4 showed about `0.157m` left/right error and nonzero near-goal ratio, but aggressive action and velocity statistics.
- Evaluated the legacy handoff checkpoint for 300 steps. It is the best available Reach baseline, reaching about `0.009m` left and `0.010m` right last-50 TCP error with `near_goal_ratio_last50` about `0.94`, though settle ratio remains zero.
- Updated `robot_simulator-main/docker/isaac/.env` for the migrated server paths under `/mnt/data2/ybd/robot_simulator` and Isaac cache root `/mnt/data2/ybd/isaac-sim-5.1`.
- Confirmed migrated baseline and Easy Reach checkpoints are present under `jz_isaac_lab/logs/rl_games/jz_bi_reach/`.
- Added `harness/context/jz_openarm_alignment.md` documenting the OpenArm-style behavior-interface hypothesis for JZ Reach.
- Added experimental Control Reach task registrations `Isaac-Reach-JZ-Bi-Control-v0` and `Isaac-Reach-JZ-Bi-Control-Play-v0`.
- Added `JZReachControlEnvCfg` and `JZReachControlEnvCfg_PLAY` using `JointPositionActionCfg(scale=0.5, use_default_offset=True)`, near-target command curriculum, and stronger smoothness/action penalties while leaving Easy v1 reward additions disabled.
- Updated RL task contracts, Docker Isaac Lab context, and project state for the migrated paths and Control task.
- Docker validation was attempted but blocked by host Docker socket permissions and unavailable passwordless sudo.
- Completed and evaluated `reach_control_64env_300it_probe` on GPU 3. Recorded that Control v0 improves smoothness dramatically but has not learned successful reaching, with left-hand error worse than baseline and zero near-goal ratio.

## 2026-07-02
- Created AI Harness entry file `agent.md`.
- Created long-term memory files `PROJECT_STATE.md`, `ROADMAP.md`, and `CHANGELOG.md`.
- Created harness directory structure:
  - `harness/tasks/`
  - `harness/contracts/`
  - `harness/context/`
  - `harness/decisions/`
- Recorded initial project phase and Isaac Lab RL bring-up baseline.
- Added `harness/context/docker_isaac_lab.md` with host/container paths, Docker service, environment variables, and runtime notes.
- Added `harness/contracts/rl_tasks.md` with RL task IDs, script entry points, CLI contracts, and checkpoint path rules.
- Added `harness/tasks/reach_training.md` with smoke training, longer training, monitoring, playback, and study order.
- Updated `PROJECT_STATE.md` to reference the new harness documentation.
- Added `harness/context/server_migration.md` with target server requirements, project copy list, Docker image migration options, `.env` path updates, verification commands, and common failure points.
- Updated `PROJECT_STATE.md` to reference the server migration guide.
- Added `harness/context/reach_task_anatomy.md` documenting the Reach task registration, scene, actions, observations, commands, rewards, termination, curriculum, and PPO configuration.
- Updated `PROJECT_STATE.md` to reference the Reach task anatomy document.
- Recorded the completed `reach_128env_2000it_v1` baseline result and headless checkpoint evaluation in `harness/tasks/reach_training.md`.
- Updated `PROJECT_STATE.md` with the baseline conclusion: reward improved, but reaching behavior is not yet successful.
- Added experimental Easy Reach task registrations `Isaac-Reach-JZ-Bi-Easy-v0` and `Isaac-Reach-JZ-Bi-Easy-Play-v0`.
- Added `JZReachEasyEnvCfg` and `JZReachEasyEnvCfg_PLAY` with reduced action scale, workspace curriculum, progress rewards, goal bonuses, and action max-absolute penalty.
- Updated RL task contracts, Reach training notes, and project state with the Easy Reach experiment.
- Recorded completed `reach_easy_128env_2000it_v1` results. Despite higher reward, numerical evaluation showed worse TCP errors and more aggressive actions than baseline.
- Updated `PROJECT_STATE.md` with the Easy v1 conclusion and marked the exact configuration as unsuitable for continuation.
- Added `harness/tasks/reach_tuning_log.md` recording the current Reach tuning rationale, baseline symptoms, planned parameter changes, evaluation criteria, and command templates.
- Updated `PROJECT_STATE.md` to reference the Reach tuning log.
- Rewrote `harness/tasks/reach_tuning_log.md` in Chinese and shortened it into a practical working log for current tuning.
